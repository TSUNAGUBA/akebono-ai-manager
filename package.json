{
  "name": "akebono-ai-manager",
  "version": "0.1.0",
  "private": true,
  "description": "AIマネージャー: 文脈を供給し、思考を促し、記録する装置 (Phase 1)",
  "engines": {
    "node": ">=22"
  },
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.8"
  }
}
